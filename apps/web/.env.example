# ENMO OS web — copy to apps/web/.env.local for `next dev`.
# Inlined at build time: set it in the build environment for `pnpm build:cf` / deploys too
# (production: https://api.enmo.marketing).
NEXT_PUBLIC_API_URL=http://localhost:4000
