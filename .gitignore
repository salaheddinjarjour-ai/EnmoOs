# dependencies
node_modules

# build output
dist
.next
.open-next
.turbo
.wrangler
**/src/generated
*.tsbuildinfo
# regenerated by `next typegen` / `next build` (web typecheck runs typegen first)
next-env.d.ts

# env
.env
.env.local
.env.*.local

# local data, test & coverage output
.data
test-results
playwright-report
coverage

# logs
*.log
